# Portable Loader

Use this file when the host does not support native skills.

Load `SKILL.md` first. If the task needs implementation details, also load:

- `references/weighting.md` for weighting methods
- `references/report-format.md` for the expected report
- `references/anti-patterns.md` before accepting a surprising result

Then perform the factor combination workflow exactly as described in `SKILL.md`.
